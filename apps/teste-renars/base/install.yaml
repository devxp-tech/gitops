---
apiVersion: v1
kind: Namespace
metadata:
  labels:
    kubernetes.io/metadata.name: teste-renars
    backstage.io/kubernetes-id: teste-renars
  name: teste-renars
spec:
  finalizers:
  - kubernetes

---
apiVersion: v1
kind: Service
metadata:
  annotations:
    external-dns.alpha.kubernetes.io/hostname: teste-renars.diegoluisi.eti.br
  labels:
    app: teste-renars
    backstage.io/kubernetes-id: teste-renars
  name: teste-renars
  namespace: teste-renars
spec:
  ports:
  - name: tcp-node
    port: 8080
  selector:
    app: teste-renars
  type: ClusterIP

---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: teste-renars
    backstage.io/kubernetes-id: teste-renars
  name: teste-renars
  namespace: teste-renars
spec:
  replicas: 1
  selector:
    matchLabels:
      app: teste-renars
  template:
    metadata:
      labels:
        app: teste-renars
        backstage.io/kubernetes-id: teste-renars
    spec:
      containers:
      - image: diegoluisi/teste-renars:3c12d96f
        imagePullPolicy: IfNotPresent
        name: teste-renars
        ports:
        - containerPort: 8080
          protocol: TCP
        resources:
          limits:
            cpu: 100m
            memory: 256Mi
          requests:
            cpu: 50m
            memory: 128Mi
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: File
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      securityContext: {}
      terminationGracePeriodSeconds: 30

---
apiVersion: autoscaling/v2beta2
kind: HorizontalPodAutoscaler
metadata:
  name: teste-renars
spec:
  maxReplicas: 3
  metrics:
  - resource:
      name: cpu
      target:
        averageUtilization: 75
        type: Utilization
    type: Resource
  minReplicas: 2
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: teste-renars

---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: teste-renars
  namespace: teste-renars
spec:
  gateways:
  - istio-system/istio-gateway
  hosts:
  - teste-renars.diegoluisi.eti.br
  http:
  - match:
    - uri:
        prefix: /
    route:
    - destination:
        host: teste-renars
        port:
          number: 8080
