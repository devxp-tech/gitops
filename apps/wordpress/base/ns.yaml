---
apiVersion: v1
kind: Namespace
metadata:
  labels:
    kubernetes.io/metadata.name: wordpress
    backstage.io/kubernetes-id: wordpress
    istio-injection: enabled
  name: wordpress
spec:
  finalizers:
    - kubernetes
