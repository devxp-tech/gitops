---
apiVersion: v1
kind: Namespace
metadata:
  labels:
    kubernetes.io/metadata.name: dioculos
    backstage.io/kubernetes-id: dioculos
  name: dioculos
spec:
  finalizers:
  - kubernetes

---
apiVersion: v1
kind: Service
metadata:
  annotations:
    external-dns.alpha.kubernetes.io/hostname: dioculos.diegoluisi.eti.br
  labels:
    app: dioculos
    backstage.io/kubernetes-id: dioculos
  name: dioculos
  namespace: dioculos
spec:
  ports:
  - name: tcp-node
    port: 8080
  selector:
    app: dioculos
  type: ClusterIP

---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: dioculos
    backstage.io/kubernetes-id: dioculos
  name: dioculos
  namespace: dioculos
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dioculos
  template:
    metadata:
      labels:
        app: dioculos
        backstage.io/kubernetes-id: dioculos
    spec:
      containers:
      - image: diegoluisi/dioculos:3c12d96f
        imagePullPolicy: IfNotPresent
        name: dioculos
        ports:
        - name: app-port
          containerPort: 8080
          protocol: TCP
        # livenessProbe:
        #   httpGet:
        #     path: /health-check/liveness
        #     port: app-port
        #   failureThreshold: 2
        #   periodSeconds: 10  
        # startupProbe:
        #   httpGet:
        #     path: /health-check/readines
        #     port: app-port
        #   failureThreshold: 30
        #   periodSeconds: 10  
        resources:
          limits:
            cpu: 100m
            memory: 128Mi
          requests:
            cpu: 50m
            memory: 64Mi
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: File
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      securityContext: {}
      terminationGracePeriodSeconds: 30

---
apiVersion: autoscaling/v2beta2
kind: HorizontalPodAutoscaler
metadata:
  name: dioculos
  labels:
    app: dioculos
    backstage.io/kubernetes-id: dioculos  
spec:
  maxReplicas: 2
  metrics:
  - resource:
      name: cpu
      target:
        averageUtilization: 75
        type: Utilization
    type: Resource
  minReplicas: 1
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: dioculos

---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: dioculos
  namespace: dioculos
  labels:
    backstage.io/kubernetes-id: dioculos
spec:
  gateways:
  - istio-system/istio-gateway
  hosts:
  - dioculos.diegoluisi.eti.br
  http:
  - match:
    - uri:
        prefix: /
    route:
    - destination:
        host: dioculos
        port:
          number: 8080
