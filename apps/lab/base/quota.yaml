---
kind: ResourceQuota
apiVersion: v1
metadata:
  name: lab-quota
  namespace: lab
  labels:
    kubernetes.io/metadata.name: lab
    backstage.io/kubernetes-id: lab
    istio-injection: enabled
spec:
  hard:
    requests.cpu: "1"
    requests.memory: 1Gi
    limits.cpu: "2"
    limits.memory: 2Gi