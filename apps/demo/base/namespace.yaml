---
apiVersion: v1
kind: Namespace
metadata:
  labels:
    kubernetes.io/metadata.name: demo
    backstage.io/kubernetes-id: demo
    istio-injection: enabled
  name: demo
spec:
  finalizers:
  - kubernetes