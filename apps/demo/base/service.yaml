---
apiVersion: v1
kind: Service
metadata:
  name: demo-stable
  namespace: demo
  annotations:
    external-dns.alpha.kubernetes.io/hostname: demo.diegoluisi.eti.br
  labels:
    app: demo
    backstage.io/kubernetes-id: demo
spec:
  type: ClusterIP
  ports:
  - name: tcp-node
    port: 8080
  selector:
    app: demo

---
apiVersion: v1
kind: Service
metadata:
  name: demo-canary
  namespace: demo
  annotations:
    external-dns.alpha.kubernetes.io/hostname: demo.diegoluisi.eti.br
  labels:
    app: demo
    backstage.io/kubernetes-id: demo
spec:
  type: ClusterIP
  ports:
  - name: tcp-node
    port: 8080
  selector:
    app: demo