---
apiVersion: v1
kind: Service
metadata:
  annotations:
    external-dns.alpha.kubernetes.io/hostname: chupa.diegoluisi.eti.br
  labels:
    app: chupa
    backstage.io/kubernetes-id: chupa
  name: chupa-stable
  namespace: chupa
spec:
  ports:
  - name: tcp-node
    port: 8080
  selector:
    app: chupa
  type: ClusterIP

---
apiVersion: v1
kind: Service
metadata:
  annotations:
    external-dns.alpha.kubernetes.io/hostname: chupa.diegoluisi.eti.br
  labels:
    app: chupa
    backstage.io/kubernetes-id: chupa
  name: chupa-canary
  namespace: chupa
spec:
  ports:
  - name: tcp-node
    port: 8080
  selector:
    app: chupa
  type: ClusterIP
