---
apiVersion: v1
kind: Service
metadata:
  annotations:
    external-dns.alpha.kubernetes.io/hostname: chupa.diegoluisi.eti.br
  labels:
    app: chupa
    backstage.io/kubernetes-id: chupa
    version: 0.0.1
  name: chupa-canary-svc
  namespace: chupa
spec:
  ports:
  - name: tcp-node
    port: 8080
  selector:
    app: chupa
  type: ClusterIP

---
apiVersion: v1
kind: Service
metadata:
  annotations:
    external-dns.alpha.kubernetes.io/hostname: chupa.diegoluisi.eti.br
  labels:
    app: chupa
    backstage.io/kubernetes-id: chupa
    version: 0.0.1
  name: chupa-stable-svc
  namespace: chupa
spec:
  ports:
  - name: tcp-node
    port: 8080
  selector:
    app: chupa
  type: ClusterIP