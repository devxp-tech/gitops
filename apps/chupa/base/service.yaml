---
apiVersion: v1
kind: Service
metadata:
  name: chupa-stable
  namespace: chupa
  annotations:
    external-dns.alpha.kubernetes.io/hostname: chupa.diegoluisi.eti.br
  labels:
    app: chupa
    backstage.io/kubernetes-id: chupa
spec:
  ports:
  - name: tcp-node
    port: 8080
  selector:
    app: chupa
  type: ClusterIP

---
apiVersion: v1
kind: Service
metadata:
  name: chupa-canary
  namespace: chupa
  annotations:
    external-dns.alpha.kubernetes.io/hostname: chupa.diegoluisi.eti.br
  labels:
    app: chupa
    backstage.io/kubernetes-id: chupa
spec:
  ports:
  - name: tcp-node
    port: 8080
  selector:
    app: chupa
  type: ClusterIP
