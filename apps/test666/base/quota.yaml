---
kind: ResourceQuota
apiVersion: v1
metadata:
  name: test666-quota
  namespace: test666
  labels:
    kubernetes.io/metadata.name: test666
    backstage.io/kubernetes-id: test666
    istio-injection: enabled
spec:
  hard:
    requests.cpu: "1"
    requests.memory: 1Gi
    limits.cpu: "2"
    limits.memory: 2Gi