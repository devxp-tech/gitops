---
apiVersion: v1
kind: Namespace
metadata:
  labels:
    kubernetes.io/metadata.name: teste-argo
  name: teste-argo
spec:
  finalizers:
  - kubernetes

---
apiVersion: v1
kind: Service
metadata:
  annotations:
    external-dns.alpha.kubernetes.io/hostname: teste-argo.diegoluisi.eti.br
  labels:
    app: teste-argo
  name: teste-argo
  namespace: teste-argo
spec:
  ports:
  - name: tcp-node
    port: 8080
  selector:
    app: teste-argo
  type: ClusterIP

---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: teste-argo
  name: teste-argo
  namespace: teste-argo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: teste-argo
  template:
    metadata:
      labels:
        app: teste-argo
    spec:
      containers:
      - image: diegoluisi/teste-argo:3c12d96f
        imagePullPolicy: IfNotPresent
        name: teste-argo
        ports:
        - containerPort: 8080
          protocol: TCP
        resources:
          limits:
            cpu: 100m
            memory: 256Mi
          requests:
            cpu: 50m
            memory: 128Mi
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: File
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      securityContext: {}
      terminationGracePeriodSeconds: 30

---
apiVersion: autoscaling/v2beta2
kind: HorizontalPodAutoscaler
metadata:
  name: teste-argo
spec:
  maxReplicas: 3
  metrics:
  - resource:
      name: cpu
      target:
        averageUtilization: 75
        type: Utilization
    type: Resource
  minReplicas: 2
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: teste-argo

---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: teste-argo
  namespace: teste-argo
spec:
  gateways:
  - istio-system/istio-gateway
  hosts:
  - teste-argo.diegoluisi.eti.br
  http:
  - match:
    - uri:
        prefix: /
    route:
    - destination:
        host: teste-argo
        port:
          number: 8080