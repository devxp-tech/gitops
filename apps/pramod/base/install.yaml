---
apiVersion: v1
kind: Namespace
metadata:
  labels:
    kubernetes.io/metadata.name: pramod
    backstage.io/kubernetes-id: pramod
  name: pramod
spec:
  finalizers:
  - kubernetes

---
apiVersion: v1
kind: Service
metadata:
  annotations:
    external-dns.alpha.kubernetes.io/hostname: pramod.diegoluisi.eti.br
  labels:
    app: pramod
    backstage.io/kubernetes-id: pramod
  name: pramod
  namespace: pramod
spec:
  ports:
  - name: tcp-node
    port: 8080
  selector:
    app: pramod
  type: ClusterIP

---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: pramod
    backstage.io/kubernetes-id: pramod
  name: pramod
  namespace: pramod
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pramod
  template:
    metadata:
      labels:
        app: pramod
        backstage.io/kubernetes-id: pramod
    spec:
      containers:
      - image: diegoluisi/pramod:3c12d96f
        imagePullPolicy: IfNotPresent
        name: pramod
        ports:
        - name: app-port
          containerPort: 8080
          protocol: TCP
        # livenessProbe:
        #   httpGet:
        #     path: /health-check/liveness
        #     port: app-port
        #   failureThreshold: 2
        #   periodSeconds: 10  
        # startupProbe:
        #   httpGet:
        #     path: /health-check/readines
        #     port: app-port
        #   failureThreshold: 30
        #   periodSeconds: 10  
        resources:
          limits:
            cpu: 100m
            memory: 128Mi
          requests:
            cpu: 50m
            memory: 64Mi
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: File
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      securityContext: {}
      terminationGracePeriodSeconds: 30

---
apiVersion: autoscaling/v2beta2
kind: HorizontalPodAutoscaler
metadata:
  name: pramod
  labels:
    app: pramod
    backstage.io/kubernetes-id: pramod  
spec:
  maxReplicas: 2
  metrics:
  - resource:
      name: cpu
      target:
        averageUtilization: 75
        type: Utilization
    type: Resource
  minReplicas: 1
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: pramod

---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: pramod
  namespace: pramod
  labels:
    backstage.io/kubernetes-id: pramod
spec:
  gateways:
  - istio-system/istio-gateway
  hosts:
  - pramod.diegoluisi.eti.br
  http:
  - match:
    - uri:
        prefix: /
    route:
    - destination:
        host: pramod
        port:
          number: 8080
