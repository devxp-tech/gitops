---
kind: ResourceQuota
apiVersion: v1
metadata:
  name: test-rollout-quota
  namespace: test-rollout
  labels:
    kubernetes.io/metadata.name: test-rollout
    backstage.io/kubernetes-id: test-rollout
    istio-injection: enabled
spec:
  hard:
    requests.cpu: "1"
    requests.memory: 1Gi
    limits.cpu: "2"
    limits.memory: 2Gi