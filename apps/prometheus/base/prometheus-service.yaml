apiVersion: v1
kind: Service
metadata:
  name: prometheus-service
  namespace: monitoring
  labels:
    run: prometheus-server
spec:
  ports:
  - port: 8080
    protocol: TCP
  selector:
    run: prometheus-server