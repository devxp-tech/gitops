---
apiVersion: v1
kind: Namespace
metadata:
  labels:
    kubernetes.io/metadata.name: alan
    backstage.io/kubernetes-id: alan
  name: alan
spec:
  finalizers:
  - kubernetes

---
apiVersion: v1
kind: Service
metadata:
  annotations:
    external-dns.alpha.kubernetes.io/hostname: alan.diegoluisi.eti.br
  labels:
    app: alan
    backstage.io/kubernetes-id: alan
  name: alan
  namespace: alan
spec:
  ports:
  - name: tcp-node
    port: 8080
  selector:
    app: alan
  type: ClusterIP

---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: alan
    backstage.io/kubernetes-id: alan
  name: alan
  namespace: alan
spec:
  replicas: 1
  selector:
    matchLabels:
      app: alan
  template:
    metadata:
      labels:
        app: alan
        backstage.io/kubernetes-id: alan
    spec:
      containers:
      - image: diegoluisi/alan:3c12d96f
        imagePullPolicy: IfNotPresent
        name: alan
        ports:
        - name: app-port
          containerPort: 8080
          protocol: TCP
        # livenessProbe:
        #   httpGet:
        #     path: /health-check/liveness
        #     port: app-port
        #   failureThreshold: 2
        #   periodSeconds: 10  
        # startupProbe:
        #   httpGet:
        #     path: /health-check/readines
        #     port: app-port
        #   failureThreshold: 30
        #   periodSeconds: 10  
        resources:
          limits:
            cpu: 100m
            memory: 128Mi
          requests:
            cpu: 50m
            memory: 64Mi
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: File
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      securityContext: {}
      terminationGracePeriodSeconds: 30

---
apiVersion: autoscaling/v2beta2
kind: HorizontalPodAutoscaler
metadata:
  name: alan
  labels:
    app: alan
    backstage.io/kubernetes-id: alan  
spec:
  maxReplicas: 2
  metrics:
  - resource:
      name: cpu
      target:
        averageUtilization: 75
        type: Utilization
    type: Resource
  minReplicas: 1
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: alan

---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: alan
  namespace: alan
  labels:
    backstage.io/kubernetes-id: alan
spec:
  gateways:
  - istio-system/istio-gateway
  hosts:
  - alan.diegoluisi.eti.br
  http:
  - match:
    - uri:
        prefix: /
    route:
    - destination:
        host: alan
        port:
          number: 8080
