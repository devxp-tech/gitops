apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "devxp-app.fullname" . }}-test-connection"
  labels:
    {{- include "devxp-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "devxp-app.fullname" . }}:{{ .Values.port }}']
  restartPolicy: Never
