---
kind: ResourceQuota
apiVersion: v1
metadata:
  name: rafael-quota
  namespace: rafael
  labels:
    kubernetes.io/metadata.name: rafael
    backstage.io/kubernetes-id: rafael
    istio-injection: enabled
spec:
  hard:
    requests.cpu: "1"
    requests.memory: 1Gi
    limits.cpu: "2"
    limits.memory: 2Gi
