apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kubecost
# resources:
#   - virtual-service.yaml
helmCharts:
  - name: cost-analyzer
    releaseName: kubecost
    repo: https://kubecost.github.io/cost-analyzer
    version: 1.102.2
    valuesInline:
      kubecostToken: aGVsbUBrdWJlY29zdC5jb20=xm343yadf98
      prometheus:
        fqdn: kubecost-prometheus-server.kubecost.svc.cluster.local
        nodeExporter:
          enabled: false
        serviceAccounts:
          nodeExporter:
            enabled: false
        kubeStateMetrics:
          enabled: false
      # global:
      #   prometheus:
      #     fqdn: kubecost-prometheus-server.kubecost.svc.cluster.local

