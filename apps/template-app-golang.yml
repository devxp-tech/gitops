apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
    name: "template-app-golang"
    annotations:
      # https://argoproj.github.io/argo-cd/operator-manual/high_availability/#webhook-and-manifest-paths-annotation
      argocd.argoproj.io/manifest-generate-paths: .
spec:
  project: dev
  source:
    repoURL: 'https://github.com/diegoluisi/argo'
    path: apps/template-app-golang/overlays/development
    targetRevision: HEAD
  destination:
    server: https://kubernetes.default.svc
    namespace: template-app-golang
  syncPolicy:
    automated:
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
