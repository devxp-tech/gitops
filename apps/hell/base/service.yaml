---
apiVersion: v1
kind: Service
metadata:
  name: hell-stable
  namespace: hell
  annotations:
    external-dns.alpha.kubernetes.io/hostname: hell.diegoluisi.eti.br
  labels:
    app: hell
    backstage.io/kubernetes-id: hell
spec:
  type: ClusterIP
  ports:
  - name: tcp-node
    port: 8080
  selector:
    app: hell

---
apiVersion: v1
kind: Service
metadata:
  name: hell-canary
  namespace: hell
  annotations:
    external-dns.alpha.kubernetes.io/hostname: hell.diegoluisi.eti.br
  labels:
    app: hell
    backstage.io/kubernetes-id: hell
spec:
  type: ClusterIP
  ports:
  - name: tcp-node
    port: 8080
  selector:
    app: hell