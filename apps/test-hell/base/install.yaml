---
apiVersion: v1
kind: Namespace
metadata:
  labels:
    kubernetes.io/metadata.name: test-hell
    backstage.io/kubernetes-id: test-hell
  name: test-hell
spec:
  finalizers:
  - kubernetes

---
apiVersion: v1
kind: Service
metadata:
  annotations:
    external-dns.alpha.kubernetes.io/hostname: test-hell.diegoluisi.eti.br
  labels:
    app: test-hell
    backstage.io/kubernetes-id: test-hell
  name: test-hell
  namespace: test-hell
spec:
  ports:
  - name: tcp-node
    port: 8080
  selector:
    app: test-hell
  type: ClusterIP

---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: test-hell
    backstage.io/kubernetes-id: test-hell
  name: test-hell
  namespace: test-hell
spec:
  replicas: 1
  selector:
    matchLabels:
      app: test-hell
  template:
    metadata:
      labels:
        app: test-hell
        backstage.io/kubernetes-id: test-hell
    spec:
      containers:
      - image: diegoluisi/test-hell:3c12d96f
        imagePullPolicy: IfNotPresent
        name: test-hell
        ports:
        - containerPort: 8080
          protocol: TCP
        resources:
          limits:
            cpu: 100m
            memory: 256Mi
          requests:
            cpu: 50m
            memory: 128Mi
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: File
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      securityContext: {}
      terminationGracePeriodSeconds: 30

---
apiVersion: autoscaling/v2beta2
kind: HorizontalPodAutoscaler
metadata:
  name: test-hell
spec:
  maxReplicas: 3
  metrics:
  - resource:
      name: cpu
      target:
        averageUtilization: 75
        type: Utilization
    type: Resource
  minReplicas: 2
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: test-hell

---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: test-hell
  namespace: test-hell
  labels:
    backstage.io/kubernetes-id: test-hell
spec:
  gateways:
  - istio-system/istio-gateway
  hosts:
  - test-hell.diegoluisi.eti.br
  http:
  - match:
    - uri:
        prefix: /
    route:
    - destination:
        host: test-hell
        port:
          number: 8080
