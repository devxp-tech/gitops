---
kind: ResourceQuota
apiVersion: v1
metadata:
  name: victor-quota
  namespace: victor
  labels:
    kubernetes.io/metadata.name: victor
    backstage.io/kubernetes-id: victor
    istio-injection: enabled
spec:
  hard:
    requests.cpu: "1"
    requests.memory: 1Gi
    limits.cpu: "2"
    limits.memory: 2Gi