---
kind: ResourceQuota
apiVersion: v1
metadata:
  name: chupa3-quota
  namespace: chupa3
  labels:
    kubernetes.io/metadata.name: chupa3
    backstage.io/kubernetes-id: chupa3
    istio-injection: enabled
spec:
  hard:
    limits.cpu: 500m
    limits.memory: 512Mi