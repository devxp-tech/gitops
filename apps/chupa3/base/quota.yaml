---
kind: ResourceQuota
apiVersion: v1
metadata:
  name: chupa3-quota
  namespace: chupa3
  labels:
    kubernetes.io/metadata.name: chupa3
    backstage.io/kubernetes-id: chupa3
    istio-injection: enabled
spec:
  hard:
    requests.cpu: "1"
    requests.memory: 1Gi
    limits.cpu: "2"
    limits.memory: 2Gi