---
kind: ResourceQuota
apiVersion: v1
metadata:
  name: fullfoda-quota
  namespace: fullfoda
  labels:
    kubernetes.io/metadata.name: fullfoda
    backstage.io/kubernetes-id: fullfoda
    istio-injection: enabled
spec:
  hard:
    requests.cpu: "1"
    requests.memory: 1Gi
    limits.cpu: "2"
    limits.memory: 2Gi