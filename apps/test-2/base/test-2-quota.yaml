---
kind: ResourceQuota
apiVersion: v1
metadata:
  name: test-2-quota
  namespace: test-2
  labels:
    kubernetes.io/metadata.name: test-2
    backstage.io/kubernetes-id: test-2
    istio-injection: enabled
spec:
  hard:
    requests.cpu: "1"
    requests.memory: 1Gi
    limits.cpu: "2"
    limits.memory: 2Gi
