---
kind: ResourceQuota
apiVersion: v1
metadata:
  name: daniboy-quota
  namespace: daniboy
  labels:
    kubernetes.io/metadata.name: daniboy
    backstage.io/kubernetes-id: daniboy
    istio-injection: enabled
spec:
  hard:
    requests.cpu: "1"
    requests.memory: 1Gi
    limits.cpu: "2"
    limits.memory: 2Gi