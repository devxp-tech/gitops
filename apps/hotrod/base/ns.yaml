apiVersion: v1
kind: Namespace
metadata:
  name: hotrod
  annotations:
    backstage.io/kubernetes-id: hotrod
    sidecar.jaegertracing.io/inject: "true"
  labels:
    app: hotrod
    app.kubernetes.io/instance: hotrod-dev
    istio-injection: enabled
    kubernetes.io/metadata.name: hotrod
spec:
  finalizers:
    - kubernetes
