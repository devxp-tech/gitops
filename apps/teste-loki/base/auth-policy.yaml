---
apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: teste-loki
spec:
  action: ALLOW
  selector:
    matchLabels:
      app: teste-loki
  rules:
  - to:
    - operation:
        paths:
        - "/"
        - "/*"
        ports:
        - "8080"
  - from:
    - source:
        ipBlocks: ["94.62.74.120/32", "5.6.7.0/24"]
