apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    app: template-java
  name: template-java
spec:
  selector:
    matchLabels:
      app: template-java
  endpoints:
    - port: http
      path: "/actuator/prometheus"
      interval: "30s"
      honorLabels: false
