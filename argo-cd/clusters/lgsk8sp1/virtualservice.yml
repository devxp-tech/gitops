apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: argocd
spec:
  gateways:
    - argocd-gateway
  hosts:
    - argocd.diegoluisi.eti.br
  http:
    - route:
      - destination:
          host: argocd-server
          port:
            number: 80
