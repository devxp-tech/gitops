apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: argo-events
spec:
  gateways:
    - argo-events
  hosts:
    - argo-events.diegoluisi.eti.br
  http:
    - route:
      - destination:
          host: webhook-eventsource-svc
          port:
            number: 12000
