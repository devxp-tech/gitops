apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: argo-events
spec:
  gateways:
    - public-gateway
  hosts:
    - argo-events.diegoluisi.eti.br
  http:
    - route:
      - destination:
          host: eventbus-default-stan-svc
          port:
            number: 4222
