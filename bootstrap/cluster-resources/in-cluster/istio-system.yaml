---
apiVersion: v1
kind: Namespace
metadata:
  name: istio-system
  labels:
    kubernetes.io/metadata.name: istio-system
    backstage.io/kubernetes-id: istio-system
    # istio-injection: enabled
spec:
  finalizers:
  - kubernetes