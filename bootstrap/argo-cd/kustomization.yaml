apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: argocd
resources:
  - github.com/argoproj-labs/argocd-autopilot/manifests/base?ref=v0.4.15
  - https://raw.githubusercontent.com/argoproj-labs/rollout-extension/v0.3.0/manifests/install.yaml
  - argo-grafana-secret-sealed.yaml # In Case of DR please comment this line
  - argocd-notifications-secret-sealed.yaml # In Case of DR please comment this line
  - github-secrets-sealed.yaml # In Case of DR please comment this line
  - sso-sealed.yaml # In Case of DR please comment this line
  - virtual-service.yaml # In Case of DR please comment this line
patchesStrategicMerge:
  - argocd-server.yaml
  - argocd-notifications-cm.yaml
patches:
  - patch: |-
      apiVersion: v1
      kind: Secret
      type: Opaque
      metadata:
        name: argocd-notifications-secret
        annotations:
          sealedsecrets.bitnami.com/managed: "true"
configMapGenerator:
  - name: argocd-cmd-params-cm
    behavior: merge
    literals:
      - server.insecure="true"
  - name: argocd-cm
    behavior: merge
    options:
      labels:
        app.kubernetes.io/part-of: argocd
    files: # In Case of DR please comment this line
      - files/dex.config # In Case of DR please comment this line
      - files/repositories # In Case of DR please comment this line
      - files/repository.credentials # In Case of DR please comment this line
      - files/resource.customizations # In Case of DR please comment this line
    literals:
      - admin.enabled=true
      - kustomize.buildOptions="--enable-alpha-plugins --enable-helm --load-restrictor LoadRestrictionsNone"
      - statusbadge.enabled=true
      - url=https://argocd.devxp-tech.io # In Case of DR please comment this line
      - accounts.backstage="apiKey, login"
  - name: argocd-rbac-cm
    behavior: merge
    options:
      labels:
        app.kubernetes.io/part-of: argocd
    literals:
      - policy.default="role:readonly"
    files:
      - files/policy.csv
components:
  # extensions controller component
  - https://github.com/argoproj-labs/argocd-extensions/manifests
