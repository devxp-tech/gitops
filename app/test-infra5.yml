apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
    name: "test-infra5"
    annotations:
      # https://argoproj.github.io/argo-cd/operator-manual/high_availability/#webhook-and-manifest-paths-annotation
      argocd.argoproj.io/manifest-generate-paths: .
spec:
  project: dev
  source:
    repoURL: 'https://github.com/diegoluisi/argo'
    path: apps/test-infra5/overlays/development
    targetRevision: HEAD
  destination:
    server: https://kubernetes.default.svc
    namespace: test-infra5
  syncPolicy:
    automated:
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
