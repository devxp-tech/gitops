apiVersion: v1
kind: Service
metadata:
  name: teste-crossplane
  labels:
    app: teste-crossplane
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: teste-crossplane