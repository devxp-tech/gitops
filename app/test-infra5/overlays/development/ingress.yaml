apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: test-infra5
  annotations:
    ingress.kubernetes.io/ssl-redirect: "false"
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: test-infra5
            port:
              number: 80