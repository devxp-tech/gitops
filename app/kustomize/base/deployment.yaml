apiVersion: apps/v1
kind: Deployment
metadata:
  name: teste-if
  labels:
    app: teste-if
    component: web
spec:
  selector:
    matchLabels:
      app: teste-if
      component: web
  template:
    metadata:
      labels:
        app: teste-if
        component: web
    spec:
      containers:
      - name: teste-if
        image: diegoluisi/teste-if:latest
        ports:
        - containerPort: 8000
        resources:
          limits:
            cpu: 512m
            memory: 512Mi
          requests:
            cpu: 256m
            memory: 256Mi