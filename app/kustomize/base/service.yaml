apiVersion: v1
kind: Service
metadata:
  name: teste-if
  labels:
    app: teste-if
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: teste-if