apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: teste-argo
  annotations:
    ingress.kubernetes.io/ssl-redirect: "false"
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: teste-argo
            port:
              number: 80