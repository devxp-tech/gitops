apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: teste-argo2
  annotations:
    ingress.kubernetes.io/ssl-redirect: "false"
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: teste-argo2
            port:
              number: 80