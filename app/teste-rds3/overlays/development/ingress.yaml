apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: teste-rds3
  annotations:
    ingress.kubernetes.io/ssl-redirect: "false"
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: teste-rds3
            port:
              number: 80