apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: monitoring
  annotations:
      prometheus.io/scrape: 'true'
      prometheus.io/port:   '3000'
spec:
  selector: 
    app: tcp-grafana
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 32000
