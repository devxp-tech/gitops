apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: argocd-ops
  namespace: argocd
spec:
  destination:
    namespace: argocd
    server: 'https://kubernetes.default.svc'
  source:
    path: clusters/lgsk8sp1
    repoURL: 'https://github.com/diegoluisi/argocd'
    targetRevision: HEAD
    directory:
      recurse: true
  project: ops
  syncPolicy:
    automated:
      prune: false
      selfHeal: false

